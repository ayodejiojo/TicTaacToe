<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lola.tictactoe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tic_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tic_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".SingleActivity" />
        <activity android:name=".Single2Activity" />
        <activity android:name=".TwoActivity" />
        <activity android:name=".Two2Activity"></activity>
    </application>

</manifest>